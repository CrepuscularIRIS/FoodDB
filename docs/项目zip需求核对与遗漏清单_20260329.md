# 项目Zip需求核对与遗漏清单（2026-03-29）

## 1. 本次核对范围
- 压缩包：`/home/yarizakurahime/data/项目文件和要求.zip`
- 文档：
  - `dataset_3_24/graph_edges_reformatted_with_product_README.md`
  - `7大类风险机理模型/*.md`

> 备注：该Zip中未发现 `.xlsx/.xls` 文件。

## 2. 已覆盖项（当前代码已实现）
1. 异构图基础数据接入：`enterprise_node.csv + graph_edges_reformatted_with_product.csv`。
2. 节点和边输出7类风险概率值。
3. 生成节点画像特征文件（可用于自动构建节点特征）。
4. 按乳制品品类做子图提取与可视化。
5. Top5%风险热点标签（节点/边）及阈值。
6. ModeA支持LLM报告生成（`/api/modela/v2/modea_report`）。
7. 前后端链路可运行（`/modela-v2`页面 + API联通）。

## 3. 当前仍是“未完全落地”的项（遗漏/待补）
1. `Hetero-GNN + Learning to Rank` 的训练版排序模型尚未实现。
2. 不确定性量化仍是工程近似（熵+证据量），未实现文档建议的Bootstrap/扰动一致性体系。
3. 资源分配当前为贪心近似，未实现完整成本收益优化器（如MILP/鲁棒优化）。
4. 机理参数库（半衰期、富集系数、温湿度产毒参数）未形成可校准版本化参数仓。
5. 代理验证闭环（历史投诉/违规等外部观测指标）尚未纳入自动评估流水线。

## 4. 关于“ModeA 与 ModeAV2 双入口”
- 已将首页 `Mode A` 默认切换为统一链路入口（嵌入 `ModelA v2` 全链路页面）。
- 统一链路核心接口：
  - `GET /api/modela/v2/view`
  - `POST /api/modela/v2/modea_report`

## 5. 建议下一步（与你当前计划一致）
1. 先做发散：Gemini 3.1 Pro 输出可建模指标候选与低成本落地策略。
2. 再做技术可行性收敛：GPT-5.4 Extended Thinking 评估可实现/难实现边界。
3. 最终做公式重构：GPT-5.4 Pro 输出可审计、可复现、可解释的评分公式与参数方案。
