# 可视化效果与安装配置教程（2026-03-31）

## 1. 本次可视化修正重点

### 1.1 Top5 子图的风险边逻辑修正

问题：
此前“仅看 Top5% 热点”模式会只保留“Top5 节点之间的边”，导致风险边数量偏少。

修正后：
- 在 Top5 模式下，保留“任一端连接 Top5 节点”的关联风险边。
- 同时自动保留这些边连接到的上下游节点，形成可研判子图。

对应前端文件：
- `frontend/app/modela-v2/page.tsx`

界面文案更新：
- `仅看Top5%节点及其关联风险边`

## 2. 可视化截图

截图目录：
- `reports/visualization_20260331/screenshots/`

文件：
1. `modela_v2_default.png`
- 默认模式（刷新图谱后）
![默认图谱](../reports/visualization_20260331/screenshots/modela_v2_default.png)

2. `modela_v2_top5_with_incident_edges.png`
- 打开“仅看Top5%节点及其关联风险边”后的子图
![Top5节点及关联风险边](../reports/visualization_20260331/screenshots/modela_v2_top5_with_incident_edges.png)

## 3. 安装与运行

### 3.1 环境建议

- Python: 3.10+
- Node.js: 18+
- npm: 9+

### 3.2 后端启动

在项目根目录执行：

```bash
cd /home/yarizakurahime/data/dairy_supply_chain_risk
python -m uvicorn backend.api:app --host 127.0.0.1 --port 8000
```

健康检查：

```bash
curl http://127.0.0.1:8000/health
```

### 3.3 前端启动

```bash
cd /home/yarizakurahime/data/dairy_supply_chain_risk/frontend
npm install
npm run dev -- --hostname 127.0.0.1 --port 3001
```

访问地址：
- `http://127.0.0.1:3001/modela-v2`

### 3.4 生产构建校验（可选）

```bash
cd /home/yarizakurahime/data/dairy_supply_chain_risk/frontend
npm run build
```

## 4. 使用步骤（复现本次图谱效果）

1. 打开 `http://127.0.0.1:3001/modela-v2`
2. 选择品类（默认首项）
3. 点击“刷新图谱”
4. 勾选“仅看Top5%节点及其关联风险边”
5. 可调“边风险阈值”观察边密度变化

## 5. 自动截图复现（可选）

说明：本次截图使用 Playwright 自动化。

脚本位置：
- `frontend/tests/capture.spec.ts`（示例）

若本地需要复现，可使用：

```bash
cd /home/yarizakurahime/data/dairy_supply_chain_risk/frontend
npm i -D playwright
```

然后运行自定义截图脚本（或按你的 CI 流程接入）。

## 6. 交付检查清单

1. `ModeA v2` 页面可打开
2. 节点风险、边风险、Top5 标记可见
3. Top5 模式下可看到 Top5 节点关联边（不再仅限 Top5-Top5 边）
4. 文档与截图已纳入项目包
